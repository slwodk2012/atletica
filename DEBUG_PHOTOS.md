# 🔍 Отладка фотографий

## Проблема: Фото не отображаются после сохранения

### Шаг 1: Проверьте консоль браузера

1. Откройте сайт: http://localhost:8000
2. Нажмите F12 (откроется DevTools)
3. Перейдите на вкладку "Console"
4. Обновите страницу (F5)
5. Проверьте, есть ли ошибки (красный текст)

### Шаг 2: Проверьте localStorage

Выполните в консоли (F12):

```javascript
// Проверить данные
const data = localStorage.getItem('trainersData');
console.log('Данные:', JSON.parse(data));

// Проверить первого тренера
const trainers = JSON.parse(data);
console.log('Первый тренер:', trainers[0]);
console.log('Основное фото:', trainers[0].image);
console.log('Дополнительные фото:', trainers[0].images);
```

### Шаг 3: Очистите localStorage и перезагрузите

Если данные повреждены:

```javascript
// Очистить все данные
localStorage.clear();

// Перезагрузить страницу
location.reload();
```

### Шаг 4: Проверьте формат данных

Правильный формат тренера:

```javascript
{
  "id": "prod-001",
  "title": "Имя тренера",
  "description": "Описание",
  "category": "Фитнес",
  "experience": "Стаж 10 лет",
  "image": "URL основного фото",  // ← Это отображается на карточке
  "imageAlt": "Тренер Имя",
  "images": ["URL1", "URL2"],     // ← Дополнительные фото
  "price": 2500,
  "currency": "RUB"
}
```

## Быстрое исправление

### Вариант 1: Ручное добавление фото

Выполните в консоли:

```javascript
// Загрузить данные
const trainers = JSON.parse(localStorage.getItem('trainersData'));

// Найти тренера (например, первого)
const trainer = trainers[0];

// Добавить основное фото
trainer.image = 'https://i.imgur.com/abc123.jpg';

// Добавить дополнительные фото
trainer.images = [
  'https://i.imgur.com/abc123.jpg',
  'https://i.imgur.com/def456.jpg'
];

// Сохранить
localStorage.setItem('trainersData', JSON.stringify(trainers));

// Перезагрузить
location.reload();
```

### Вариант 2: Сброс к исходным данным

```javascript
// Удалить сохраненные данные
localStorage.removeItem('trainersData');

// Перезагрузить (загрузятся данные из products.json)
location.reload();
```

## Проверка после сохранения

После сохранения тренера в админке:

1. Откройте консоль (F12)
2. Выполните:

```javascript
// Проверить последнего сохраненного тренера
const trainers = JSON.parse(localStorage.getItem('trainersData'));
const lastTrainer = trainers[trainers.length - 1];
console.log('Последний тренер:', lastTrainer);
console.log('Основное фото:', lastTrainer.image);
console.log('Есть ли фото?', lastTrainer.image ? 'ДА' : 'НЕТ');
```

3. Если `image` пустое или undefined - фото не сохранилось
4. Проверьте, заполнили ли вы поле "Основное фото"

## Типичные ошибки

### Ошибка 1: Поле "Основное фото" пустое

**Решение:**
- Обязательно заполните поле "Основное фото"
- Это поле отмечено звездочкой (*)
- Без него карточка не будет отображаться

### Ошибка 2: URL неправильный

**Правильные URL:**
```
✅ https://i.imgur.com/abc123.jpg
✅ https://images.unsplash.com/photo-123
✅ images/trainer.jpg
✅ data:image/jpeg;base64,/9j/4AAQ...
```

**Неправильные URL:**
```
❌ imgur.com/abc123 (нет https://)
❌ https://imgur.com/abc123 (это страница, не изображение)
❌ C:\Users\photo.jpg (локальный путь Windows)
```

### Ошибка 3: Не обновили страницу

**Решение:**
- После сохранения ОБЯЗАТЕЛЬНО нажмите F5
- Или Ctrl+R (Windows) / Cmd+R (Mac)

## Тестирование

### Тест 1: Проверка загрузки данных

```javascript
fetch('data/products.json')
  .then(r => r.json())
  .then(d => {
    console.log('Загружено из JSON:', d.products.length);
    console.log('Первый тренер:', d.products[0]);
  });
```

### Тест 2: Проверка отображения карточек

```javascript
// Проверить, сколько карточек на странице
const cards = document.querySelectorAll('.card');
console.log('Карточек на странице:', cards.length);

// Проверить первую карточку
const firstCard = cards[0];
const img = firstCard.querySelector('.card__image');
console.log('Первая карточка:', {
  title: firstCard.querySelector('.card__title').textContent,
  image: img.src,
  imageLoaded: img.complete
});
```

### Тест 3: Проверка изображения

```javascript
// Проверить, загружается ли изображение
const testUrl = 'https://i.imgur.com/abc123.jpg';
const img = new Image();
img.onload = () => console.log('✅ Изображение загружено');
img.onerror = () => console.log('❌ Ошибка загрузки');
img.src = testUrl;
```

## Полный сброс

Если ничего не помогает:

```javascript
// 1. Очистить все
localStorage.clear();

// 2. Удалить cookies
document.cookie.split(";").forEach(c => {
  document.cookie = c.replace(/^ +/, "").replace(/=.*/, "=;expires=" + new Date().toUTCString() + ";path=/");
});

// 3. Перезагрузить
location.reload();
```

## Получение помощи

Если проблема не решается:

1. Откройте консоль (F12)
2. Выполните:

```javascript
// Собрать информацию для отладки
const debug = {
  trainersCount: JSON.parse(localStorage.getItem('trainersData') || '[]').length,
  cardsCount: document.querySelectorAll('.card').length,
  firstTrainer: JSON.parse(localStorage.getItem('trainersData') || '[]')[0],
  errors: []
};

console.log('Debug info:', debug);
```

3. Скопируйте вывод и отправьте для анализа
